// http://jsbasics.geekbrains.ru

let radius = 80;

drawCircle(radius, radius, radius, 'black');

drawCircle(radius * 0.5, radius * 0.8, 80 * 0.25, 'white');
drawCircle(radius * 0.5, radius * 0.8, radius * 0.125, 'black');

drawCircle(radius * 1.5, radius * 0.8, 80 * 0.25, 'white');
drawCircle(radius * 1.5, radius * 0.8, radius * 0.125, 'black');
